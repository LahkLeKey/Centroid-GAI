name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build build --config Release
      - name: Test
        run: ctest --test-dir build -C Release --output-on-failure

  native-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.9'
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install Clang tools
        run: |
          sudo apt-get update && sudo apt-get install --yes clang-tidy
          python -m pip install clang-format==21.1.0
      - name: Build Node addon and download matching headers
        working-directory: persistence
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Build native addon
        working-directory: persistence/api
        run: bun run native:build
      - name: Test Node addon
        working-directory: persistence/api
        run: bun run native:test
      - name: Configure native checks
        run: cmake -S . -B build-lint
      - name: Lint all C sources and check formatting
        run: cmake --build build-lint --target cgai_lint cgai_format_check

  documentation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Doxygen
        run: sudo apt-get update && sudo apt-get install --yes doxygen
      - name: Configure documentation
        run: cmake -S . -B build-docs -DCGAI_BUILD_DOCS=ON -DCGAI_BUILD_TESTS=OFF
      - name: Enforce documentation standard
        run: cmake --build build-docs --target docs
      - uses: actions/upload-artifact@v4
        with:
          name: api-documentation
          path: build-docs/docs/html
          if-no-files-found: error

  web-and-integration:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CGAI_API_URL: http://127.0.0.1:3000
      VITE_API_PROXY_TARGET: http://127.0.0.1:3000
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.9'
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install workspace dependencies
        working-directory: persistence
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Typecheck persistence packages
        working-directory: persistence
        run: bun run typecheck
      - name: Lint and build web application
        working-directory: persistence/web
        run: |
          bun run lint
          bun run build
      - name: Install browser and system dependencies
        working-directory: persistence/web
        run: bunx playwright install --with-deps chromium
      - name: Build and start deployable API with migrated PostgreSQL
        run: docker compose -p centroid-gai-ci up --detach --build --wait --wait-timeout 180
      - name: Test API against PostgreSQL
        working-directory: persistence/api
        run: bun run api:test
      - name: Test browser workflows against native API
        working-directory: persistence/web
        run: bun run test:e2e
      - name: Save deployable web bundle
        uses: actions/upload-artifact@v4
        with:
          name: web-dist
          path: persistence/web/dist
          if-no-files-found: error
      - name: Capture service logs
        if: always()
        run: docker compose -p centroid-gai-ci logs --no-color > compose-ci.log 2>&1
      - name: Save failure diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: integration-diagnostics
          path: |
            compose-ci.log
            persistence/web/test-results
          retention-days: 7
      - name: Stop isolated CI services
        if: always()
        run: docker compose -p centroid-gai-ci down --volumes --remove-orphans
